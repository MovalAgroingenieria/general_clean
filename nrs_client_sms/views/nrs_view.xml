<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Droplist action (res.partner) -->
    <act_window id="action_nrs_wizard"
        context="{'mode': 'partner'}"
        name="Send SMS"
        res_model="nrs.wizard"
        src_model="res.partner"
        key2="client_action_multi"
        view_mode="form"
        target="new"
        groups="nrs_client_sms.group_send_nrs_sms"/>

    <!-- Droplist action (account.invoice) -->
    <act_window id="action_nrs_wizard_invoice"
        context="{'mode': 'invoice'}"
        name="Send SMS"
        res_model="nrs.wizard"
        src_model="account.invoice"
        key2="client_action_multi"
        view_mode="form"
        target="new"
        groups="nrs_client_sms.group_send_nrs_sms"/>

</odoo>