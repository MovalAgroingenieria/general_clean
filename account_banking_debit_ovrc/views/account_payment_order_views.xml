<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Account config setting OVRc options -->
    <record id="account_config_settings_ovrc_view" model="ir.ui.view">
        <field name="name">ovrc.account_config_settings.view.form</field>
        <field name="model">account.config.settings</field>
        <field name="inherit_id" ref="account.view_account_config_settings"/>
        <field name="arch" type="xml">
            <xpath expr="//separator[@name='analytic_account']" position="before">
                <group name="ovrv" string="Castellon Virtual Colletion Office (OVRc)">
                    <field name="agency"/>
                </group>
            </xpath>
        </field>
    </record>

    <!-- Add banking debit OVRc fields to account payment order form -->
    <record id="account_payment_order_form_ovrc" model="ir.ui.view">
        <field name="name">account.payment.order.form.ovrc</field>
        <field name="model">account.payment.order</field>
        <field name="inherit_id" ref="account_payment_order.account_payment_order_form"/>
        <field name="arch" type="xml">

            <!-- Lock date_prefered field -->
            <xpath expr="//field[@name='date_prefered']" position="attributes">
                <attribute name="attrs" add="{'readonly':[('payment_mode_name','=', 'OVRc')]}"/>
            </xpath>

            <!-- OVRc options -->
            <xpath expr="//group[@name='head']" position="after">
                
                <group string="OVRc options" name="ovrc" col="2" attrs="{'invisible':[('payment_mode_name', '!=', 'OVRc')]}">
                    <group name="ovrc-left">
                        <field name="payment_mode_name" invisible="1"/>
                        <field name="agency" attrs="{'required':[('payment_mode_name','=', 'OVRc')]}"/>
                        <field name="income_type" attrs="{'required':[('payment_mode_name','=', 'OVRc')]}"/>
                        <field name="concept_code" attrs="{'required':[('payment_mode_name','=', 'OVRc')]}"/>
                    </group>
                    <group name="ovrc-right">
                        <field name="exaction_type" attrs="{'required':[('payment_mode_name','=', 'OVRc')]}"/>
                        <field name="receipt_period" attrs="{'required':[('payment_mode_name','=', 'OVRc')]}"/>
                        <field name="error_mode" attrs="{'required':[('payment_mode_name','=', 'OVRc')]}"/>
                    </group>
                </group>
            </xpath>

            <!-- Add errors tap -->
            <xpath expr="//page[@name='bank-payment-lines']" position="after">
                <page name="errors" string="Errors" attrs="{'invisible':['|',('payment_mode_name', '!=', 'OVRc'), ('error_mode', '!=', 'permissive')]}">
                    <field name="errors_found" nolabel="1"/>
                </page>
            </xpath>

        </field>
    </record>

</odoo>