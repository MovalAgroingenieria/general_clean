<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">Add separate fields for street type</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='street']" position="before">
                <field name="street_type_id" class="oe_edit_only" style="margin-right: 2%;"
                    attrs="{'readonly': [('type', '=', 'contact'),('parent_id', '!=', False)]}"
                    placeholder="Street type..." context="{'in_combo': True}"
                    options="{'no_open': True, 'no_quick_create':True, 'no_create_edit':True}"/>
                <field name="street_type_show" class="oe_inline oe_read_only" style="float: left; margin-right: 2%;"/>
            </xpath>

        </field>
    </record>

    <record id="view_partner_address_form" model="ir.ui.view">
        <field name="name">Add separate fields for street type (edition form)</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_address_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='street']" position="before">
                   <field name="street_type_id" placeholder="Street type..."
                       context="{'in_combo': True}" class="oe_edit_only"
                       options="{'no_open': True,'no_quick_create':True, 'no_create_edit':True}"/>
                   <field name="street_type_show" class="oe_read_only" style="margin-right: 2%;"/>
            </xpath>
        </field>
    </record>

</odoo>