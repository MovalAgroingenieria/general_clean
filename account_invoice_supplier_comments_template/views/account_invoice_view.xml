<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Add invoice comments to vendor bill -->
    <record model="ir.ui.view" id="invoice_form_add_comment_supplier">
        <field name="name">account.invoice.supplier.form.comment</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_supplier_form"/>
        <field name="arch" type="xml">
            <xpath expr="/form/sheet/notebook" position="inside">
                <page string="Comments" name="comments">
                    <p style="margin-top: 10px;">
                        The comments will be displayed on the printed document.
                        You can load a predefined template, write your own text
                        or load a template and then modify it only for this document.
                    </p>
                    <group string="Top Comments">
                        <field name="supplier_comment_template1_id" string="Load a template"
                            domain="[('position','=','before_lines')]" context="{'default_position': 'before_lines'}"/>
                        <field name="note_supp1ier1" nolabel="1" colspan="2"/>
                    </group>
                    <group string="Bottom Comments">
                        <field name="supplier_comment_template2_id" string="Load a template"
                            domain="[('position','=','after_lines')]" context="{'default_position': 'after_lines'}"/>
                        <field name="note_supp1ier2" nolabel="1" colspan="2"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

</odoo>
